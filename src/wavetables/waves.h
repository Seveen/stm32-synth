#ifndef _WAVES_H
#define _WAVES_H

#include <stdint.h>

extern uint16_t antiarctable[256];
extern uint16_t arctable[256];
extern uint16_t sintable[256];
extern uint16_t tritable[256];
extern uint16_t sawtable[256];

#endif